import Branding from "./Branding";

const SignImage = () => {
  return (
    <div className="hidden min-h-screen w-[50%] flex-col items-center justify-center bg-[#0F0F0F] text-white lg:flex">
      <Branding fontSize="5xl" />
    </div>
  );
};

export default SignImage;
